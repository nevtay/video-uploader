import {
  SET_FILENAME,
  SET_FILETYPE,
  SET_VIDEO_FILE,
  SET_VIDEO_TITLE,
  SET_VIDEO_DATE,
  SET_VIDEO_TIME,
  SET_VIDEO_LOCATION,
} from "./types";

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
